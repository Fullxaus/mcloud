apiVersion: v1
kind: Pod
metadata:
  name: webapp-pod
  labels:
    app: webapp
spec:
  containers:
  - name: webapp
    image: mcloud-webapp:latest
    imagePullPolicy: Never
    ports:
    - containerPort: 8080
